# Student-Record-Management-System
This project is a SQL-based Student Record Management System used to store and analyze student data. It includes queries to calculate average marks, find top performers, group students by course and city, and assign grades using basic SQL operations like GROUP BY, aggregate functions, and CASE statements.
